{
	"name": "noeyetest",
	"version": "4.3.0",
	"description": "BBGM Prog Script — 4.3.0 unpublished candidate (live = 3.2.x / progbox v3.2.1; port of progbox v4.3)",
	"main": "src/NoEyeTest.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"lint": "biome check --write .",
		"format": "biome format --write .",
		"ci:lint-format": "pnpm run lint && pnpm run format"
	},
	"author": "fearandesire",
	"license": "MIT",
	"dependencies": {
		"semantic-release": "^19.0.5"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4"
	}
}
